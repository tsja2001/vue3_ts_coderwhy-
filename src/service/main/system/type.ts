export interface IUserListParamas {
  size: number
  offset: number
}
 export interface IUserInfo {
  name?: string,
  realname?: string,
  password?: string,
  cellphone?: string,
  roleId?: string,
  departmentId?: string,
 }
